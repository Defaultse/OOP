package problem3;

//import java.util.Scanner;

public class Main {

	public static void main(String[] args) {
		// Analyzer compute = new Analyzer();
		/*
		 * Data data1; System.out.println("Enter number(Q to quit:"); Scanner input =
		 * new Scanner(System.in); String command = input.nextLine(); do { int number =
		 * Integer.parseInt(command); data1 = new Data(number); }
		 * while(!command.contains("Q")); double ans1 = data1.getMax(); double ans2 =
		 * data1.getAverage();
		 * 
		 * System.out.println(ans1 + " " + ans2);
		 */
		     
	     Analyzer data = new Analyzer();
	     data.Analyze();
	     
	}

}
